spring:
  datasource:
    # Puerto 5432 = Transaction pooler | Puerto 6543 = Session pooler
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DATABASE_USERNAME:postgres.mnwzoaqjvvovuziqsqwd}
    password: ${DATABASE_PASSWORD:Ingenieroinformatico2020}
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  level:
    com.inventario: DEBUG
    org.springframework.security: INFO
